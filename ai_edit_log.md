Issue found in AI-generated test: test_load_wrong_top_level_type passed a raw string to the write_deck helper, which writes strings verbatim. The file was invalid JSON, so the test exercised the JSON-parse error path instead of the wrong-type path it was named for — and left line 81 uncovered. Fix: pass a bare int (42) so the helper serializes it via json.dumps, producing valid JSON of the wrong shape.
